#include "main.h"
/**
 * print_rev - function that prints a string in reverse
 * @s: pointer
 */
void print_rev(char *s)
{
	int l, len, i;

	for (l = 0; l[s] != '\0'; l++)
	{
		len = l[s];
	}

	for (i = len - '1'; i >= 0; i--)
	{
		_putchar(i[s]);
	}
	_putchar('\n');
}
