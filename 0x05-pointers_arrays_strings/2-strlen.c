#include "main.h"
/**
 * _strlen - function that returns the lenght of a sring
 * @s: pointer string
 * Return: lenght
 */
int _strlen(char *s)
{
	int len;

	for (len = 0; len[s] != '\0'; len++)
	;

	return (len);
}
