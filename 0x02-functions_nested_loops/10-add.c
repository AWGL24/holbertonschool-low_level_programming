#include "main.h"
/**
 * add - function that adds two integers and return teh result
 * @one: parameter
 * @two: parameter
 * Return: sum
 */
int add(int one, int two)
{
	int sum;

	sum = one + two;

	return (sum);
}
